{
  "name": "bold-bose",
  "version": "1.0.0",
  "description": "NestJS + Next.js (Refine + Antd) Monorepo Server Project",
  "scripts": {
    "dev:server": "npm run start:dev --prefix server",
    "dev:admin": "npm run dev --prefix admin",
    "install:server": "npm install --prefix server",
    "install:admin": "npm install --prefix admin",
    "install:all": "npm run install:server && npm run install:admin"
  }
}
